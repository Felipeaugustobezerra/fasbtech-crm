import { CONTRACT_STATUS_LABELS, formatContractDate } from "@/components/contracts/contract-format";
import type { ContractDetails } from "@/lib/contracts/queries";

export function ContractDetailsView({ contract, clientName, templateName }: Readonly<{ contract: ContractDetails; clientName?: string; templateName?: string }>) {
  const content = contract.snapshot?.content ?? (typeof contract.draft_data.content === "string" ? contract.draft_data.content : "");
  return <div className="space-y-6"><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="flex flex-wrap items-center justify-between gap-3"><h2 className="text-lg font-semibold text-slate-950">Informação</h2><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-700">{CONTRACT_STATUS_LABELS[contract.status]}</span></div><dl className="mt-5 grid gap-5 sm:grid-cols-2 lg:grid-cols-3"><div><dt className="text-xs font-semibold uppercase tracking-wide text-slate-500">Cliente</dt><dd className="mt-1 text-sm text-slate-900">{clientName ?? "Cliente histórico"}</dd></div><div><dt className="text-xs font-semibold uppercase tracking-wide text-slate-500">Template</dt><dd className="mt-1 text-sm text-slate-900">{contract.snapshot?.template.name ?? templateName ?? "Template histórico"}</dd></div><div><dt className="text-xs font-semibold uppercase tracking-wide text-slate-500">Atualizado</dt><dd className="mt-1 text-sm text-slate-900">{formatContractDate(contract.updated_at)}</dd></div><div><dt className="text-xs font-semibold uppercase tracking-wide text-slate-500">Gerado</dt><dd className="mt-1 text-sm text-slate-900">{formatContractDate(contract.generated_at)}</dd></div><div><dt className="text-xs font-semibold uppercase tracking-wide text-slate-500">Enviado</dt><dd className="mt-1 text-sm text-slate-900">{formatContractDate(contract.sent_at)}</dd></div><div><dt className="text-xs font-semibold uppercase tracking-wide text-slate-500">Assinado</dt><dd className="mt-1 text-sm text-slate-900">{formatContractDate(contract.signed_at)}</dd></div></dl></section><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><h2 className="text-lg font-semibold text-slate-950">{contract.snapshot ? "Snapshot imutável" : "Conteúdo em preparação"}</h2><div className="mt-4 whitespace-pre-wrap rounded-lg bg-slate-50 p-4 text-sm leading-7 text-slate-800">{content || "Sem conteúdo."}</div></section></div>;
}
